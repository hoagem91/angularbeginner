Angular
day 1+2:
-- ng g c <new folder>
day 3:data & eventbinding
- string enpolation : {{ <obj> }}
- data binding : [type] = "'text'" || "inputType"
- event binding : (<event>) = "handler()"
day4: ngIf directive 
- *ngIf:tạo điều kiện
- ng-template:tạo 1 template có thể thực thi dưới điều kiện nào đó
day5: ngFor directive
- *ngFor:tạo một vòng lặp - "let <prop> of <obj>"
- variable của ngForOf:
+ $implicit T: giá trị của phần tử ở lần lặp hiện tại  
day6:Class & Style Binding
- class: bind - [class.ten-class] , [class] = "array , [class] = "object"  
- style: bind - [style.ten-style] = "<prop>" , style.temStyle.unit (style.fonstSize.em = 2) , [style] = "<object>"
day7: inputbinding
- @input():có thể binding từ ngoài vào
- oninit:khi call class sẽ gọi hàm init và chỉ chạy một lần từ lần gọi
- onchanges: chạy khi thay đổi input
day8:outputbinding
- @output() : parent có thể nhận được sự kiện từ thằng con = eventEmitter<obj>()
day9:twowaybinding
-[(<>)] : vừa binding vừa nhận hàm thực thi từ con
day10:ViewChild & ViewChildrent
- #<>: tham chiếu tới 1 component or 
- @ViewChild('<string>'||component , {static:true, read:}) : tham chiếu tới 1 view childcomponent - static không chạy được khi component nằm trong structure directive
- @ViewChildrent(component || '<string>') : dung khi co nhieu component cung tac vu nhung lai nam trong structure directive
- ngDoCheck()
day11:typescript
- type: never = throw exception
- 4 poperty oop:
  + tính đóng gói : (chỉ có thuộc tính public thì mới truy cập được)
  + tính kế thừa : kế thừa thuộc tính của lớp cha
  + tính đa hình : con có thể ghi đè lại method của lớp cha
  + tính trừu tượng hoá :chỉ hiển thị những phần quan trọng abstract	
day12:typescript advance
- type unknow : giống kiểu dữ liệu any nhưng yêu cầu chặt chẽ hơn về kiểm tra kiểu dữ liệu
- & : intersection type kế thừa lại các method or poperty
- union type : string | number - hoặc các type khác
- Exclude<t,u> : loại bỏ những types được gắn cho U và lấy T
- Extract<t,u> : lấy những types được được gắn cho U và loại T
- readonly<T>: chỉ được đọc chứ không được set
- Partial<T> : biến tất cả các thược tính T -> ?.(optional)
- Nullable<T> : { () | null} biến tất cả các poperty có thể là null
- Pick<t,k extends keyof T> : pick từ trong t những type có key là K
- Record<k extends keyof any,T> : gán type key T cho từng key của k
- returnType<t> : lấy kiểu dữ liệu của 1 fnc trả về
- omit : ket hop guawx exclude va pick
day13: Content Projection
- <ng-content> : chứa render body
- select : document.queryselector - truy xuất tới những element,component cần thực hiện
- ngProjectAs : giúp element select tới element khác - poperty thuộc ng-container
day14 : ngTemplate
- ng-template : được call ở template ref (thằng gọi) || <ng-container> call ngTemplate = [<nameTmpl>]
- [ngTemplateOutLet] = "<nameTmpl>"
- [ngTemplateOutLetContext] = {"poperty"}
- hai binding trên nằm trong ng-container và được call qua let- trong ng-template
day15: dependency injection
- hiểu là class A có thể phụ thuộc vào nhiều class khác nhưng lại có thuộc tính của class B
day16: dependency injection angular
- use provider : để inject component - provide:<nameComponentInject>
day17: contentchild&contentchildren
- contentchild : ex điển hình là lấy templateRef của component con 
- contentchildren : ex là theo dõi sự thay đổi của thằng con qua việc lấy queryList
- directive : target đến ngtemplate có kèm thêm attribute
day18:pipe
- use pipe : {{<> | namePipe}} dùng để nhận một input và return output nào đó
- file đuôi .pipe
day19: RxJS Concepts
- observable : có thể hiểu như một dòng chảy - là 1 function 
- observer : là tập hợp callback có 3 agrs(next,error or complete) được gửi đến bởi observable
- use observable : chỉ cần subcribe vào nó là được
day20:RxJS Creation Operator
- of(<>).subcribe(observe):để tạo 1 observe từ bất kì type nào
- from(<>): để tọa 1 obsever từ 1 iterable(string<tách chuỗi>,array,map/set) or promise
- formEvent(): dùng để chuyển đổi 1 event sang observe - không tự unsubcribe 
- fromEventPattern(): tạo observe từ 1 event 
khác fromEvent:
// fromEvent() từ ví dụ trên
// output: MouseEvent {...}
fromEvent(btn, 'click').subscribe(observer);

// fromEventPattern
// output: MouseEvent {...}
fromEventPattern(
  (handler) => {
    btn.addEventListener('click', handler);
  },
  (handler) => {
    btn.removeEventListener('click', handler);
  }
).subscribe(observer); 
- defer(): nhận một observeFactory -> observe mới
const now$ = defer(() => of(Math.random()));
// output: 0.27312186273281935
now$.subscribe(observer);
// output: 0.7180321390218474
now$.subscribe(observer);
// output: 0.9626312890837065
now$.subscribe(observer);
- throw error("an error") : tạo observe và show error ngay lập tức
day21:RxJS Transformation | Pipe Operator
- .pipe(map()).subcribe(observe) : khác với type map không phải là 1 vòng lặp mà là khi chạy qua các phần tử mới thì tạo 1 observe mới
- .pipe(pluck()): lấy ra 1 phần tử để tạo 1 observe
- mapTo() : định nghĩa một observe được trả về type gì
- reduce(arr,curr=>{},init) : tạo ra 1 observe với giá trị trả về trong reduce khi ct complete thì nó mới emit 
- scan(arr,curr=>{},init) : tạo ra observe với từng giá trị trả về trong reduce
- toArray() : tạo observe trả về type array
- buffer() :Lưu trữ giá trị được emit ra và đợi đến khi closingNotifier emit thì emit những giá trị đó thành 1 array -- click time để hiện val theo từng giây
- bufferTime() : tạo observe trả về theo y/c buffTime -- bufferTime(2000) -> [0,1] , [2,3]
day22:RxJS Filtering
- pipe.(filter(<fcn>)) : emit 1 observable giá trị trả về đúng với đk 
- first(<>) : emit giá trị observable đầu tiên thoả mãn
- last(<>) :emit giá trị observable cuối cùng thao mãn
- find(<fcn>) : emit giá trị observable đầu tiên thoả mãn
- single(<>) : emit 1 giá trị observable và báo lỗi nêu như có 2 gt tm trả về undefind khi không tim thây
- take() : nhận vào tham số count để dùng đặt dk được emit sau đó complete , != first() không báo lỗi khi không tm
- takeLast() : lấy n giá trị cuối cùng sau dk 
- takeUntil() : ví dụ trong việc unsubcribe một fromEvent
- takeWhile(<dk>) : lấy n giá trị thoả mãn điều kiện
- skip(n) : bỏ qua n phần tử
- skipUntil(<event>) : 1 notifier báo hiệu điểm skip trả về giá trị sau skip
- skipWhile(<dk>) :1 notifier báo hiệu điểm skip trả về giá trị thỏa mãn và sau skip
- distinct() : so sánh giá trị được emit từ observable và emit những giá trị chưa được emit qua
- distinctUntilChanged() : so sánh giá trị được emit từ observable và bỏ qua những giá trị trùng lặp cạnh nhau  
- auditTime(<time>) : emit giá trị thỏa mãn trong time
- throttleTime(<time>) : runtime khi emit nhận giá trị mới  
			0s -> 0 runtime 1500 
			1s -> 1 runtime 500
			(1.5s) -> 1 runtime over
			2s -> 2 runtime 1500
			3s -> 3 runtime 500
			3.5s -> 3 runtime over
			..vv
- debounceTime(<time>) : runtime không cần phải emit giá trị mới
day23:Combination Operator
- forkJoin():emit tất cả các giá trị đã được complete 
- combineLastest(): emit giá trị có khoảng thời gian dài nhất đầu tiên sau đó chạy như bth - trong 1s có thể emit 3 giá trị
- zip() : emit các index giá trị giá trị tương ứng của 2-3 object thành một array
- concat() : emit từ hai obj thành 1
- merge() : emit các giá trị cho đến khi complete thì dừng
- race() : emit giá trị nào complete nhanh hơn
- withLastestFrom()<1pipe>:trong ví dụ click nó sẽ emit giá trị click theo thời gian 
- startWith() <1pipe> : ép phải bắt đầu bằng giá trị trong startWith
- endWith() <1pipe>: ngược lại với start
- pairwise() :
day24:RxJS ErrorHanding
- catchError(): emit một handle để bắt lỗi
- retry():không báo error mà thay vào đó sẽ retry lại số lần offer
- defaultValue() : emit một giá trị lỗi
- throwIfEmpty(fcn) : emit một hàm trả về giá trị để bắt lỗi
- every(fcn) : emit một hàm trả về giá trị boolean
- iif(fcn->boolean,trueObs,falseObs) === toán tử ba ngôi : emit giá trị trả về theo trueObs<fcn> or falseObs<fcn> và khi khời tạo sẽ check qua cả trueObs và falseObs
day25 : Higher order observable
- higher order observable : Observe<observe>
- mergeAll() , concatAll() , switchAll() : giúp hai obs lồng nhau thành 1 
-- mergeAll(3) : ví dụ chạy 3 lần click liên tục 
-- concatAll(3) : sub obs1 complete -> obs2 -> obs3 
-- switchAll() : sub obs mới bỏ qua cái obs cũ
- mergeMap() | mergeAll + map 
- concatMap() | concatAll + map
- switchMap() | switchAll + map
- exhaustMap() : cancel các obs sau obs được chạy
- merge|concat|switchMapTo() :  
- tap() : tạo giá trị mới mà không ảnh hưởng đến data base
- finalize()
- repeat() : lặp lại
- timeInterval() : see khoảng cách time 
- timeout()
- timeoutWith()
- toPromise() :